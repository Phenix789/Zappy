#include <iostream>
#include "parser.hpp"

bool	parser::parse_command(std::string &str, std::list<std::string> &list)
{
  std::clog << str << std::flush;
  return (true);
}

bool	parser::interpret_command(std::list<std::string> &list, data_thread &data)
{
  return (true);
}

